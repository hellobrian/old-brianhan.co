import styled from 'styled-components';

export const PageRoot = styled.div`
  display: block;
  margin: 0 auto;
`;

export const Main = styled.div`
  position: relative;
  z-index: 1;
  padding-bottom: 3rem;
  padding-top: 3rem;
`;
